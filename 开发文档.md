# 智识资讯 · 开发文档

> 一个每天自动抓取全网 AI 资讯、用大模型智能分类整理、以苹果官网风格呈现的资讯日报网站。

---

## 1. 项目概述

「智识资讯」是一个"AI 资讯日报"产品，核心能力：

1. **自动采集**：从 9 个权威科技媒体 RSS 源抓取当天新闻。
2. **智能整理**：调用 DeepSeek 大模型，对素材做「AI 相关性筛选 → 六分类 → 一句话摘要 → 多源交叉验证」。
3. **前端展示**：苹果官网风格的响应式网页，含今日热门总览、分板块卡片、换一批、AI 聊天助手。
4. **自动/手动更新**：每天 08:00 定时刷新（macOS launchd），也可点「立即更新」按钮手动刷新（本地服务）。

---

## 2. 技术架构

```
┌─────────────┐   抓 RSS   ┌──────────────┐   调 API   ┌──────────────┐
│  9 个新闻源  │ ────────▶ │ generate.py  │ ────────▶ │  DeepSeek    │
│ (RSS/XML)   │            │  (Python)    │            │  (大模型)    │
└─────────────┘            │  采集+整理   │ ◀──────── │              │
                           └──────┬───────┘  返回JSON  └──────────────┘
                                  │ 写 data/news.json
                                  │ 写 data/news.js
                                  ▼
                    ┌──────────────────────────┐
                    │  index.html（前端）       │
                    │  加载 news.js 渲染        │
                    │  · 热门总览 · 分板块卡片  │
                    │  · 换一批 · 聊天助手      │
                    └──────────────────────────┘
                                  ▲
                    ┌─────────────┴─────────────┐
                    │  server.py（本地服务）     │
                    │  托管静态 + /api/refresh   │
                    └───────────────────────────┘
```

- **纯静态前端 + 无框架**：`index.html` 单文件，原生 JS，无构建步骤。
- **后端极简**：`generate.py` 只依赖 Python 标准库（无第三方依赖）；`server.py` 用标准库 `http.server`。
- **数据契约**：前后端通过 `data/news.json`（或 `news.js`）解耦。

---

## 3. 目录结构

```
AI CASE/
├── index.html                 # 前端（单文件，含 HTML+CSS+JS）
├── index-带动画版本.html       # 前端变体（标题背景动画版，非默认）
├── generate.py                # 核心：采集 + 大模型整理，产出数据
├── server.py                  # 本地服务：托管页面 + /api/refresh 接口
├── config.json                # 配置（DeepSeek key 等，已在 .gitignore 中）
├── data/
│   ├── news.json              # 生成的新闻数据（JSON）
│   ├── news.js                # 生成的新闻数据（JS，供 file:// 双击打开）
│   └── config.js              # 生成的前端配置（含 key，勿提交）
├── com.user.aidaily.refresh.plist  # launchd 定时任务（每天 08:00）
├── deploy.sh                  # 腾讯云服务器一键部署脚本
├── .github/workflows/daily-refresh.yml  # GitHub Actions 定时刷新
├── README.md                  # 部署说明
└── .gitignore
```

---

## 4. 数据流（核心）

### 4.1 generate.py 的处理流程

`generate.py` 是整个项目的核心。入口 `main()` 调用 `run()` + `write_outputs()`：

```
run()
 ├─ 1) 抓取：遍历 FEEDS，fetch() 抓 RSS，parse_feed() 解析
 │       ├─ 过滤「汇总式」标题（含 ；或 ｜ 的多条新闻）
 │       └─ 过滤旧闻（parse_pubdate() 判断，超过 2 天丢弃）
 │
 ├─ 2) 整理：把素材（标题|来源）分批发给 DeepSeek（chat()）
 │       └─ SYSTEM_PROMPT 要求：筛选 AI 相关 + 分 6 类 + 摘要 + hot 标记
 │
 ├─ 3) 组装：
 │       ├─ 大模型结果为主
 │       ├─ 关键词兜底（classify() 按 CATEGORY_RULES，板块不足时补齐）
 │       └─ 每板块内 hot 排序，截断到 MAX_PER_CATEGORY 条
 │
 └─ 返回 data（{date, updatedAt, news[]}）

write_outputs(data)
 ├─ 写 data/news.json
 ├─ 写 data/news.js（window.__NEWS__ = {...}）
 └─ 写 data/config.js（window.__CONFIG__ = {...}，供前端聊天调 DeepSeek）
```

### 4.2 新闻数据结构

`data/news.json` 的每条新闻（`news[]` 元素）：

```json
{
  "category": "硬件与芯片",     // 六个板块之一
  "title": "苹果 Mac mini M6 发布",
  "summary": "一句话摘要",        // 列表卡片显示
  "detail": "2-3 句展开说明",    // 详情弹层显示
  "source": "极客公园",          // 来源
  "date": "08-26",              // MM-DD
  "url": "https://...",         // 原文链接
  "hot": true                   // 多源交叉验证标记（重要新闻）
}
```

### 4.3 六个板块

`CATEGORIES`：`硬件与芯片 / 模型与产品 / 游戏AI / 机器人具身智能 / 行业政策 / 资本与市场`。前后端共用，改这里需要同步改 `index.html` 的 `SECTION_DEFS`。

---

## 5. 关键配置

### 5.1 config.json（本地，不入库）

```json
{
  "api_key": "sk-xxx",
  "base_url": "https://api.deepseek.com",
  "model": "deepseek-chat"
}
```

- `generate.py` 优先读环境变量 `DEEPSEEK_API_KEY`（供 GitHub Actions 用），否则读 `config.json`。
- `data/config.js` 由 `write_outputs()` 从 `config.json` 生成，供前端聊天助手直接调 DeepSeek。

### 5.2 新闻源（generate.py 的 FEEDS）

9 个 RSS 源：极客公园、IT之家、游戏陀螺、36氪快讯（经 RSSHub）、爱范儿、雷科技、少数派、掘金、人民网（IT/财经）。

> 注意：人民网 RSS 更新慢（旧闻），已被「超过 2 天过滤」规则排除，当前实际贡献为 0，可考虑替换成更新的官方源。

### 5.3 大模型 Prompt（generate.py 的 SYSTEM_PROMPT）

定义了筛选标准、六分类、摘要/detail 字段、多源交叉验证（hot）规则。调整分类口径、摘要长度、hot 判定都在这里改。

### 5.4 其他参数

| 参数 | 默认值 | 含义 |
|---|---|---|
| `MAX_PER_CATEGORY` | 18 | 每板块最多条数（= 6 条/批 × 3 批） |
| `BATCH_SIZE` | 200 | 每次发给大模型的素材条数 |
| `PER_PAGE`（前端） | 6 | 每批每板块显示条数 |

---

## 6. 本地开发

### 6.1 启动

```bash
cd "AI CASE"
python3 server.py          # 启动本地服务
# 浏览器打开 http://localhost:8000
```

- 点右上角「立即更新」→ 触发 `/api/refresh` → 后端实时抓取+整理 → 返回最新数据。
- 更新一次约 20~60 秒（取决于源响应和大模型）。

### 6.2 直接跑数据生成（不启动服务）

```bash
python3 generate.py
```

只生成 `data/news.json` / `news.js` / `config.js`。

### 6.3 双击打开（降级方式）

若不想启动服务，可双击 `index.html`（通过 `news.js` 的 `<script>` 方式加载数据）。但此方式下「立即更新」按钮不可用（无后端）。

---

## 7. 定时任务

**macOS 本地**：`com.user.aidaily.refresh.plist`（launchd），每天 08:00 跑 `generate.py`。

```bash
# 加载/卸载
launchctl load ~/Library/LaunchAgents/com.user.aidaily.refresh.plist
launchctl unload ~/Library/LaunchAgents/com.user.aidaily.refresh.plist
```

**GitHub Actions**：`.github/workflows/daily-refresh.yml`，`cron: 0 0 * * *`（UTC 0 点 = 北京时间 8 点），用 `secrets.DEEPSEEK_API_KEY`。

---

## 8. 部署

- **GitHub Pages + Actions**：push 到 GitHub，Actions 每天刷新，Pages 发布静态页。详见 `README.md`。
- **腾讯云服务器**：`deploy.sh` 一键部署（装 nginx + crontab 每天 8 点）。

---

## 9. 二次开发指南

### 加一个新闻源
在 `generate.py` 的 `FEEDS` 列表加一行 `{"name": "源名", "url": "RSS地址"}` 即可。要求该源返回标准 RSS 2.0 或 Atom XML。

### 改分类 / 加板块
1. `generate.py`：改 `CATEGORIES`、`SYSTEM_PROMPT`、`CATEGORY_RULES`。
2. `index.html`：改 `SECTION_DEFS`（kicker/title/desc）、`CATEGORY_RULES` 的颜色对应（`.chip.xxx` CSS）。

### 调前端交互
`index.html` 内关键函数：
- `render()` / `renderHotHTML()` / `renderSectionHTML()`：渲染逻辑
- `bindSection()` / `bindAll()`：交互绑定（卡片点击、换一批、滚动渐入）
- `refreshNow()`：立即更新
- 聊天助手：`buildContext()` / `askDeepSeek()` / `sendChat()`

### 调大模型行为
改 `generate.py` 的 `SYSTEM_PROMPT`，以及 `chat()` 里的 `temperature`、`max_tokens`。

---

## 10. 已知问题 / 待优化

1. **人民网源过时**：RSS 内容是 2021 年的，当前靠日期过滤排除，建议换新源。
2. **关键词兜底质量**：大模型漏掉的板块靠 `classify()` 关键词补齐，偶有误分类。
3. **重复新闻**：同一事件多源报道时，大模型去重不彻底（如 DeepSeek 营收出现两条）。
4. **动画版本未启用**：`index-带动画版本.html` 是变体，默认 `index.html` 无动画。
5. **前端 key 暴露**：`data/config.js` 含 key，仅适合本地；公开部署需把聊天改成后端中转（`/api/chat`）。
